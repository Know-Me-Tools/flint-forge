apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flint-forge.fullname" . }}-config
  labels:
    {{- include "flint-forge.labels" . | nindent 4 }}
data:
  DATABASE_URL: {{ include "flint-forge.databaseUrl" . | quote }}
