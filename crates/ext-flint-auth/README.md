# ext-flint-auth

This capability is **pure SQL** and ships as a SQL-only Postgres extension
(`images/postgres18/extensions/`), not a compiled pgrx extension. The `src/lib.rs`
here is a reference stub; no Rust build is required for flint-auth.
