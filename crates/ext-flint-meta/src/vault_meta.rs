//! vault_meta — Vault key metadata stub.
//!
//! The `flint_meta.vault_keys` and `flint_meta.vault_key_assignments` tables
//! are defined in `sql/flint_meta.sql`. Rust functions over these tables will
//! be added in Phase 2. In Phase 1 they serve as read-only metadata for the
//! `ext-flint-vault` extension's key-assignment queries.
