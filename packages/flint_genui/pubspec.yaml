name: flint_genui
description: Flint A2UI component catalog for Flutter, extending flutter/genui without Gemini/Firebase lock-in
version: 0.1.0
homepage: https://github.com/prometheus-ags/flint-forge

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # No assets or fonts bundled; consumed by host app
