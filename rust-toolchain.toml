[toolchain]
# p15-c001: pgrx 0.18.1 requires rustc >= 1.96. The wasmtime 46 security upgrade
# already required stable >= 1.94, so this pin is consistent with recent toolchain
# decisions. MSRV is now 1.96 in Cargo.toml.
channel = "1.96"
components = ["rustfmt", "clippy"]
