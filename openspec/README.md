# OpenSpec change sets

Each change is a directory with `proposal.md` (why/what/contract) and `tasks.md` (ordered,
checkable steps ending at a hard gate). Build in order; halt at every gate. Phase 0 below.
