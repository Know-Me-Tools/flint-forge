# p0-c001 — Tasks

- [x] Create `[workspace]` Cargo.toml (members + exclude pgrx crates).
- [x] `forge-domain`: TenantId, SubjectId, Json, ForgeError.
- [x] `forge-identity`: Claims, RlsContext (subject/tenant), verify_and_build (todo), outbound_headers.
- [x] `forge-policy`: Decision, Request, Pep trait.
- [x] `fdb-domain` + `fdb-ports` (five traits) compile.
- [x] `fke-domain` + `fke-ports` (four traits) compile.
- [x] `fdb-gateway` + `fke-server` expose `/healthz`.
- [x] Workspace lint policy: `[workspace.lints]` pedantic + justified allows; per-crate opt-in.
- [x] Dagger `check` pipeline (`.dagger/main.go` + `dagger.json`) wrapping `scripts/ci-check.sh`:
      fmt --check, clippy::pedantic -D warnings, cargo check.
- [x] GATE: `./scripts/ci-check.sh` green (fmt + clippy::pedantic -D warnings + cargo check). PASSED.

## Gate evidence (toolchain 1.90)
```
==> rustfmt --check          OK
==> clippy (pedantic, -D warnings)   0 warnings across 22 crates
==> cargo check              Finished
OK: fmt + clippy::pedantic + cargo check all green
```

## Notes / carried forward
- pgrx extensions (`ext-flint-*`) are excluded from the workspace gate; they build via
  `cargo pgrx` and are validated in p0-c002.
- `.dagger/internal` + `go.mod` are generated by `dagger develop` (CLI absent at scaffold time);
  the wrapped checks are proven locally.
